{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install --upgrade pip --break-system-packages && pip install numpy==1.26.4 --break-system-packages && pip install -r requirements.txt --break-system-packages"
  },
  "deploy": {
    "startCommand": "gunicorn app_nodb:app",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}